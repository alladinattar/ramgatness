foo=$(xxd -b file | cut -d" " -f 2-7 | tr "\n" " " )
for (( i=0; i<${#foo}; i++ )); do
  echo "${foo:$i:1}";
  if [[ "${foo:$i:1}" = 1 ]]; then
    stress-ng --class memory --memfd 1 --memfd-byte 100% --timeout 1s

  else
    continue
  fi
  sleep 0.5
done
